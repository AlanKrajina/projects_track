<p> Here are your projects, <%= @user.username %></p>
<br>


<% if !@user.projects.empty? %>


  <% @user.projects.each do |project| %>
    <div>
      <h2><%= project.name %></h2>
      <br>
      <%= project.content %>
      <br>
      <%= project.completion %>
      <br><br>

  <a href="/projects/<%=project.id%>/edit" class="button">Edit Project </a>
  <br><br>

  <form method="post" action="/projects/<%=project.id%>/delete">
    <input id="hidden" type="hidden" name="_method" value="DELETE">
    <input type="submit" class="button" value="Delete Project">
  </form>
  <br>

    </div>
  <% end %>


<% else %>
    <h2> Currently there are no projects available.</h2>

<% end %>



<p> and your score from current and past projects</p>

  <% current_user.scores.each_with_index do |score,index| %>

    <p><%= "Project #{index+1}. => #{score.grade}" %></p>
  <% end %>

  <form method="post" action="/scores/delete">
    <input id="hidden" type="hidden" name="_method" value="DELETE">
    <input type="submit" class="button" value="Remove grades">
  </form>
